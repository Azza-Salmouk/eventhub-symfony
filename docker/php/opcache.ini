[opcache]
; Disabled in dev — OPcache file locks cause "Directory not empty" errors
; on Windows NTFS bind-mounts when Symfony tries to swap cache directories.
; Enable this only in production (APP_ENV=prod).
opcache.enable=0
opcache.enable_cli=0
