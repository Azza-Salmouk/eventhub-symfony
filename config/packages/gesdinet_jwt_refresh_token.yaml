gesdinet_jwt_refresh_token:
    refresh_token_class: App\Entity\RefreshToken
    ttl: 2592000          # 30 days
    ttl_update: true      # sliding expiry — reset TTL on each use
    single_use: false
